pip install ansible
pip install docker-py
pip install molecule --pre	

molecule init role --role-name foo
molecule init scenario --role-name foo --scenario-name new-scenario
molecule test
molecule check 
molecule check --scenario-name foo	
molecule --debug check
molecule destroy
molecule destroy --all
molecule destroy --scenario-name foo
molecule converge --driver-name foo
molecule --debug destroy
molecule idempotence
molecule idempotence --scenario-name foo
molecule --debug idempotence
molecule init role --role-name foo
molecule init scenario --scenario-name default --role-name foo

molecule init template --url https://example.com/user/cookiecutter-repo
molecule lint
molecule lint --scenario-name foo
molecule --debug lint
molecule test
molecule test --scenario-name foo
molecule --debug test
molecule verify
molecule verify --scenario-name foo
molecule --debug verify

##
molecule init role --role-name ci6 --driver-name docker
molecule init role --driver-name docker
pip install --upgrade docker-py

g/^$/d

mail  -s "test" noreply@gmail.com

nohup java -jar /usr/share/jenkins/jenkins.war > /dev/null 2>&1&

apt-get install software-properties-common vim gcc curl 
add-apt-repository ppa:webupd8team/java
apt-get update
apt-get install oracle-java8-installer
 
wget -q -O - https://pkg.jenkins.io/debian/jenkins-ci.org.key | apt-key add -
sh -c 'echo deb http://pkg.jenkins.io/debian-stable binary/ > /etc/apt/sources.list.d/jenkins.list'
apt-get update
apt-get install jenkins

user-data
curl http://169.254.169.254/latest/user-data/
curl http://169.254.169.254/latest/meta-data/instance-id  
curl http://169.254.169.254/latest/meta-data/placement/availability-zone

mount -t nfs4 -o nfsvers=4.1,rsize=1048576,wsize=1048576,hard,timeo=600,retrans=2 $(curl http://169.254.169.254/latest/meta-data/placement/availability-zone).fs-baee4813.efs.us-west-2.amazonaws.com:/ efs



